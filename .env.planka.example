# ==============================================================================
# OpenZero — Planka Environment Configuration Template
# ==============================================================================
# Rename this file to .env.planka and fill in your secrets.
# This file specifically configures the self-hosted Kanban board.

# --- URLs & Networking ---
# The public or Tailscale URL where you will access Planka.
BASE_URL=http://localhost:1337

# --- Database ---
# Must match the DB credentials in your main .env file.
# Format: postgresql://[user]:[password]@[host]:[port]/[db_name]
DATABASE_URL=postgresql://zero:CHANGE_ME_STRONG_PASSWORD@postgres:5432/zero_db

# --- Security ---
# A random, long string of characters used for sessions. 
# You can generate one with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_RANDOM_64_CHAR_STRING

# --- Default Admin Account ---
# These credentials will be used for the first-time login at http://localhost:1337
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=CHANGE_ME_PLANKA_PASS
DEFAULT_ADMIN_NAME=Admin
DEFAULT_ADMIN_USERNAME=admin

# --- Miscellaneous ---
# Set to 1 if you are running behind a reverse proxy (e.g. Nginx).
# For Tailscale/Direct access, leave as 0.
TRUST_PROXY=0
